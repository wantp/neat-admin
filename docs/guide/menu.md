# 菜单管理

## 菜单权限

通常的场景为：
- 不同用户的的菜单列表不同
- 用户不能通过浏览器地址栏输入地址跳转到没有权限的菜单(即前端路由权限)

##### 配置菜单权限
菜单权限基于角色控制，为菜单配置一个或多个角色，前端调用[菜单树]()，接口根据登录用户的角色返回对应的树形菜单，前端根据返回数据展示菜单栏、同时调整前端路由权限。


## 菜单嵌套
通过配置菜单的父级菜单可以获得嵌套的菜单结构


## 菜单排序
使用拖拽来排序菜单